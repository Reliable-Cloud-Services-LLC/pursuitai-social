__pycache__/
*.pyc
.pytest_cache/
.venv/
.DS_Store
.env

# Rendered media is uploaded to object storage, not committed (W8).
# Instagram fetches by public URL; the repo is not that host.
assets/cards/
assets/screenshots/
assets/video/
assets/preview/
assets/linkedin/
assets/instagram/

# assets/icons/ IS committed: it is a source asset (rasterised once by
# scripts/build_icons.py), not generated media. Committing it keeps the
# engine free of any SVG/cairo runtime dependency.
